const Event = require('../../models/event');
const User = require('../../models/user');
const axios = require('axios');
const { transformEvent } = require('./merge');

module.exports = {
  createTrip: async (args, req) => {
    console.log('estamos en la accion');


  }
};